{% extends 'base.html.twig' %}

{% block title %}ListFriend{% endblock %}

{% block body %}
    <h1>ListFriend</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ list_friend.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('list_friend_index') }}">back to list</a>

    <a href="{{ path('list_friend_edit', {'id': list_friend.id}) }}">edit</a>

    {{ include('list_friend/_delete_form.html.twig') }}
{% endblock %}
