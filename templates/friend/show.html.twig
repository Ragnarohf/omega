{% extends 'base.html.twig' %}

{% block title %}Friend{% endblock %}

{% block body %}
    <h1>Friend</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ friend.id }}</td>
            </tr>
            <tr>
                <th>Username_1</th>
                <td>{{ friend.username1 }}</td>
            </tr>
            <tr>
                <th>Username_2</th>
                <td>{{ friend.username2 }}</td>
            </tr>
            <tr>
                <th>Is_pending</th>
                <td>{{ friend.isPending ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('friend_index') }}">back to list</a>

    <a href="{{ path('friend_edit', {'id': friend.id}) }}">edit</a>

    {{ include('friend/_delete_form.html.twig') }}
{% endblock %}
